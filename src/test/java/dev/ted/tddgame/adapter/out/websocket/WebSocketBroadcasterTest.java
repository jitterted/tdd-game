package dev.ted.tddgame.adapter.out.websocket;

class WebSocketBroadcasterTest {

    // given a game that has started
    // when gameUpdate(game) happens
    // then HTML (customized for Player1) is sent to Player1

    // 1. Need Inbound Handler to associated Player with the WebSocketSession (and the Game)
    // PlayerConnections?
    //  tracks tuple of WebSocketSession + Game + Player
}